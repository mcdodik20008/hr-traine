# HR Training Bot - Инструкция по запуску

## Предварительные требования

- Docker и Docker Compose
- Python 3.11+
- Токен Telegram бота (получить у [@BotFather](https://t.me/BotFather))
- API ключ Google Gemini (получить на [ai.google.dev](https://ai.google.dev)) **или**
- API ключ GigaChat (получить на [developers.sber.ru](https://developers.sber.ru/)) - используется как fallback

---

## 🚀 Быстрый старт

### 1. Настройка окружения

Создайте виртуальное окружение и установите зависимости:

```bash
# Создать и активировать venv
python -m venv venv
.\venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка `.env` файла

Скопируйте `.env.example` в `.env` и заполните:

```bash
BOT_TOKEN=your_telegram_bot_token_here
GEMINI_API_KEY=your_gemini_api_key_here

# Опционально: GigaChat как fallback (если Gemini недоступен)
GIGACHAT_API_KEY=your_gigachat_api_key_here
# Или используйте OAuth2:
# GIGACHAT_CLIENT_ID=your_client_id
# GIGACHAT_CLIENT_SECRET=your_client_secret

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=hr_traine
POSTGRES_HOST=db
POSTGRES_PORT=5432
```

> **Важно:** 
> - Замените `your_telegram_bot_token_here` и `your_gemini_api_key_here` на реальные ключи!
> - Если Gemini недоступен в вашем регионе, настройте GigaChat как fallback
> - Бот автоматически переключится на GigaChat, если Gemini не работает

### 3. Запуск базы данных

```bash
docker-compose up -d db
```

Подождите 5-10 секунд, пока PostgreSQL полностью запустится.

### 4. Применение миграций

```bash
python -m alembic upgrade head
```

Вы должны увидеть:
```
INFO  [alembic.runtime.migration] Running upgrade  -> 6cee592f9ee6, Add time tracking
INFO  [alembic.runtime.migration] Running upgrade 6cee592f9ee6 -> 20251207_onboarding
INFO  [alembic.runtime.migration] Running upgrade 20251207_onboarding -> 20251215_collection_flow
INFO  [alembic.runtime.migration] Running upgrade 20251215_collection_flow -> 20251219_candidates
✅ Added 5 professional candidates to database
```

### 5. Загрузка тестовых данных

**Загрузить учебные шаги (13 лабораторных):**
```bash
python -m app.scripts.seed_labs
```

> **Примечание:** Профессиональные кандидаты уже добавлены через миграцию `20251219_candidates`!

**🆕 5.5. Инициализация RAG Real-Time Coach:**

```bash
python -m app.scripts.initialize_rag
```

Это создаст FAISS индекс с базой знаний для Real-Time Coach (53 документа).
Вы должны увидеть:
```
✅ RAG system initialized successfully!
📊 Documents: 53
```

**Доступные кандидаты в системе:**

📊 **IT Специалисты (из миграции):**
- 👨‍💻 **Иван Петров** - Senior Java Developer (4 года опыта, Target)
- 👩‍💻 **Наталья Смирнова** - Frontend Developer Angular (3.5 года, Evasive)
- 👩‍💼 **Екатерина Волкова** - Database Administrator (5 лет, Target)
- 👩‍💻 **Римма Козлова** - Программист 1С (4.5 года, Silent)
- 👨‍💻 **Дмитрий Соколов** - Python Developer (3 года, Toxic)

📝 **Базовые кандидаты (опционально через seed):**
```bash
python -m app.scripts.seed_candidates
```
Добавит:
- Мария Петрова (Target)
- Дмитрий Токсиков (Toxic)  
- Иван Молчунов (Silent)
- Ольга Туманная (Evasive)

### 6. Запуск бота

**Вариант A: Локальный запуск (для разработки)**
```bash
python -m app.bot.main
```

**Вариант B: Через Docker Compose (полный стек)**
```bash
docker-compose up -d
```

---

## 📋 Проверка работы

### Проверить статус контейнеров
```bash
docker ps
```

Должны быть запущены:
- `hr_traine_db` (PostgreSQL)
- `hr_traine_bot` (если запускали через docker-compose)

### Проверить логи бота
```bash
docker-compose logs -f bot
```

### Проверить логи БД
```bash
docker-compose logs db
```

---

## 🧪 Тестирование функционала

### Базовые команды бота

1. `/start` - Регистрация пользователя
2. `/onboarding` - Пройти онбординг (alias `/labs`)
3. `/interview` - Начать интервью с кандидатом
4. `/expert` - Панель эксперта (для проверки заданий)

### Тест Фазы 1

**1. Контроль времени:**
- Начните `/onboarding` (или `/labs` для совместимости)
- Завершите шаг очень быстро
- Должно появиться предупреждение о слишком быстром выполнении

**2. LLM-валидация:**
- Загрузите Excel-файл в задании FILE_UPLOAD
- Должна сработать автопроверка + LLM-анализ
- Увидите обратную связь с найденными проблемами

**3. Интерактивное интервью:**
- `/interview`
- Выберите профессионального кандидата (Java, Angular, DBA, 1С, Python)
- Поздоровайтесь с кандидатом
- Задайте несколько вопросов
- Попрощайтесь - бот автоматически сгенерирует отчет с оценками!

**🆕 4. Real-Time Coach (RAG):**
> **Требует:** `python -m app.scripts.initialize_rag`

Во время интервью попробуйте разные типы вопросов:

- ⚠️ **Запрещенные:** "Сколько вам лет?" → получите предупреждение о ТК РФ
- 💡 **Плохая техника:** "Вы умеете работать в команде?" → подсказка использовать открытые вопросы  
- ✅ **Хорошие:** "Расскажите о сложном проекте" → одобрение техники STAR

Coach дает feedback **ДО** ответа кандидата:
```
👤 Вы: Сколько вам лет?

🤖 Coach: ⚠️ Вопросы о возрасте могут быть дискриминационными по ст. 3 ТК РФ

🤖 Кандидат: Мне 28 лет.
```

Подробнее: `docs/RAG_TESTING.md`

**Доступные психотипы:**
- 🎯 **Target** (Целевой) - конструктивный, развернутые ответы
- ☠️ **Toxic** (Токсичный) - конфликтный, негативный
- 🤐 **Silent** (Молчаливый) - односложные ответы
- 🌫️ **Evasive** (Уклончивый) - расплывчатые ответы

---

## 🔧 Устранение неполадок

### База данных не запускается

```bash
# Проверить логи
docker-compose logs db

# Пересоздать БД
docker-compose down -v
docker-compose up -d db
```

### Ошибка миграции "type vector does not exist"

БД создана, но расширение pgvector не включено. Исправлено в миграции `6cee592f9ee6`.

Решение:
```bash
docker-compose down -v
docker-compose up -d db
python -m alembic upgrade head
```

### Бот не отвечает

1. Проверьте `BOT_TOKEN` в `.env`
2. Убедитесь, что бот запущен: `docker-compose logs bot`
3. Проверьте сеть: `docker network ls`

### LLM не работает

1. **Проверьте API ключи в `.env`:**
   - `GEMINI_API_KEY` - основной провайдер
   - `GIGACHAT_API_KEY` или `GIGACHAT_CLIENT_ID`/`GIGACHAT_CLIENT_SECRET` - fallback

2. **Проверьте логи при старте:**
   ```
   ✅ LLM model ready: gemini-1.5-flash (GEMINI)  # Gemini работает
   ✅ LLM model ready: GigaChat (GIGACHAT)         # Используется GigaChat fallback
   ⚠️ LLM model not initialized...                 # Проблема
   ```

3. **Если Gemini недоступен в регионе:**
   - Бот автоматически переключится на GigaChat (если настроен)
   - Или используйте VPN для доступа к Gemini
2. Убедитесь, что ключ активен на [ai.google.dev](https://ai.google.dev)
3. Проверьте лимиты API

---

## 🗄️ Работа с базой данных

### Создание новой миграции

```bash
python -m alembic revision --autogenerate -m "Описание изменений"
```

### Откат миграции

```bash
python -m alembic downgrade -1  # Откатить на 1 шаг назад
```

### Полный сброс БД (создание с нуля)

```bash
# Шаг 1: Удалить все данные и контейнеры
docker-compose down -v

# Шаг 2: Пересоздать БД
docker-compose up -d db

# Шаг 3: Подождать 5-10 секунд запуска PostgreSQL
sleep 10

# Шаг 4: Накатить ВСЕ миграции (включая кандидатов)
python -m alembic upgrade head

# Шаг 5: Загрузить учебные шаги
python -m app.scripts.seed_labs

# Шаг 6 (опционально): Добавить базовых тестовых кандидатов
python -m app.scripts.seed_candidates

# Готово! Профессиональные кандидаты уже в БД через миграцию
```

> **Важно:** Миграция `20251219_candidates` автоматически добавляет 5 профессиональных IT-кандидатов при первом запуске!

### Подключение к БД напрямую

```bash
docker exec -it hr_traine_db psql -U postgres -d hr_traine
```

Полезные SQL-команды:
```sql
-- Список таблиц
\dt

-- Описание таблицы
\d users

-- Запросы
SELECT * FROM users;
SELECT * FROM lab_steps;
SELECT * FROM candidate_profiles;

-- Выход
\q
```

---

## 🔄 Обновление проекта

После изменения кода:

```bash
# Если изменились зависимости
pip install -r requirements.txt

# Если изменились модели БД
python -m alembic revision --autogenerate -m "Описание"
python -m alembic upgrade head

# Перезапустить бота
docker-compose restart bot
# или
python -m app.bot.main
```

---

## 📂 Структура проекта

```
hr-traine/
├── app/
│   ├── bot/
│   │   ├── handlers/       # Обработчики команд
│   │   ├── states.py       # FSM состояния
│   │   └── main.py         # Точка входа бота
│   ├── core/
│   │   ├── llm_client.py   # Gemini API клиент
│   │   └── search_map.py   # Валидатор Excel
│   ├── rag/                # 🆕 RAG Real-Time Coach
│   │   ├── vector_store.py # FAISS векторное хранилище
│   │   ├── embeddings.py   # Генерация эмбеддингов
│   │   └── coach.py        # Логика коучинга
│   ├── data/
│   │   ├── knowledge/      # 🆕 База знаний (53 документа)
│   │   └── rag_index/      # 🆕 FAISS индекс
│   ├── database/
│   │   ├── models.py       # SQLAlchemy модели
│   │   └── base.py         # БД подключение
│   └── scripts/
│       ├── seed_labs.py    # Загрузка лабораторий
│       ├── seed_candidates.py  # Загрузка кандидатов
│       └── initialize_rag.py   # 🆕 Инициализация RAG
├── alembic/
│   └── versions/           # Миграции БД
├── docker-compose.yml      # Docker конфигурация
├── requirements.txt        # Python зависимости
└── .env                    # Переменные окружения
```

---

## 🎯 Реализованные фичи (Фаза 1)

- ✅ **Контроль времени** - отслеживание слишком быстрого/медленного выполнения
- ✅ **LLM-валидация** - автоматическая проверка логики карты поиска
- ✅ **Психотипы кандидатов** - 4 типа поведения (Target, Toxic, Silent, Evasive)
- ✅ **Пошаговое обучение** - 13 лабораторных заданий
- ✅ **Экспертная оценка** - ручная проверка с баллами 1-5

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи бота: `docker-compose logs bot`
2. Логи БД: `docker-compose logs db`
3. Переменные окружения в `.env`
4. Статус контейнеров: `docker ps`

---

**Разработано для системы обучения HR-интервьюеров** 🎓
